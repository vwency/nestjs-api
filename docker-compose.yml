services:
  postgres:
    build:
      context: .
      dockerfile: database/Dockerfile
    image: ${PG-IMAGE}:${IMAGE-TAG}
    restart: always
    shm_size: 128mb
    
    environment:
      POSTGRES_USERNAME: ${DATABASE_USERNAME}
      DATABASE_NAME: ${DATABASE_NAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      PGDATA: ${PGDATA}
      PGPORT: ${DATABASE_PORT}
      TZ: "Europe/Moscow"
      
    ports:
      - "${DATABASE_PORT}:${DATABASE_PORT}"
    container_name: "postgres"
    volumes:
      - pg_data:${PGDATA}
    networks:
      - backend
      
    logging:
      driver: "json-file" 
      options:
        max-size: "1m"  
        max-file: "3" 
        
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $DATABASE_USERNAME"]
      interval: 30s
      timeout: 10s
      retries: 5        
      start_period: 5s

    mem_limit: 2G
    cpus: 4.0
  

  nestjs:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${NESTAPI-IMAGE}:${IMAGE-TAG}
    env_file:
      - prod.env
    environment:
      TZ: "Europe/Moscow"
    
    ports:
      - "${BACKEND_PORT}:${BACKEND_PORT}"  
    depends_on:
      - postgres 
    container_name: "nestjs-app"
    networks:
      - backend
      
    restart: always
    
    logging:
      driver: "json-file" 
      options:
        max-size: "1m"  
        max-file: "3" 

    mem_limit: 2G
    cpus: 4.0
        
    sysctls:
      net.core.somaxconn: 1024
      net.ipv4.tcp_syncookies: 1


volumes:
  pg_data:

networks:
  backend:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 192.168.100.0/24


